{
  "name": "mongo-prompt-library",
  "version": "0.1.0",
  "description": "MongoDB as your agent's prompt library — versioned, A/B-testable, tenant-overridable, semantically retrievable prompts. A plug-and-copy template for building agents.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "bun run --watch src/server.ts",
    "start": "bun run src/server.ts",
    "export": "bun run src/export.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "mongodb": "^6.17.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0"
  }
}
